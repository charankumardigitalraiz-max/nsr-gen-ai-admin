# Local dev — Vite proxy forwards /api and /uploads → local backend (port 5002)
VITE_API_URL=/api
# VITE_DEV_API_TARGET=http://localhost:5002

# Production (admin/.env.production — images load from Render):
# VITE_API_ORIGIN=https://nsr-gen-ai-backend.onrender.com
